$("#post-form").html("<%= escape_javascript(content_tag(
  :div,'投稿が保存されました', class: "alert alert-success")
)%>");
$("#display-post").html("<%= escape_javascript(render(
  partial: 'display_post',
  locals: { creator: @post.user, post: @post }
))%>");
$("#logo")[0].scrollIntoView({behavior: "smooth"});

