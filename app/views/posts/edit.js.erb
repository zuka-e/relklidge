$("#post-form").html("<%= escape_javascript(render(
  partial: 'post_form',
  locals: { post: @post, tags: @tags }
  ))%>");
$("#post-form")[0].scrollIntoView({behavior: "smooth"});
$("#startTimer").prop("disabled", true);
$("#resetTimer").prop("disabled", true);
