$("#comment-form").html("<%= escape_javascript(render(partial: 'comment_form.html.erb', locals: { creator: @comment.user }))%>");
$("#comment-form")[0].scrollIntoView({behavior: "smooth", block: "center"});
